# ===========================================
# PRODUCTION DEPLOYMENT ON DIGITALOCEAN
# ===========================================
# 1. Copy this file: cp .env.example .env
# 2. Update all values below with your actual credentials
# 3. NEVER commit .env to git
# 4. Use strong passwords in production
# 5. Keep your API keys secure
#
# For complete deployment instructions, see docs/DEPLOYMENT.md
# ===========================================

# Database Configuration
# Option 1: Use external Neon database (recommended for production)
# DATABASE_URL=postgresql://username:password@host.neon.tech:5432/dbname?sslmode=require

# Option 2: Use local PostgreSQL in Docker (default for DigitalOcean deployment)
DATABASE_URL=postgresql://postgres:CHANGE_ME_STRONG_PASSWORD@db:5432/ar_automation
POSTGRES_DB=ar_automation
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_PORT=5432

# API Keys
OPENAI_API_KEY=sk-your-openai-api-key-here
LANGCHAIN_API_KEY=your-langchain-api-key-here
LANGCHAIN_TRACING_V2=false
LANGCHAIN_PROJECT=ar-automation

# Application Environment
ENVIRONMENT=production
NODE_ENV=production

# Port Configuration
FRONTEND_PORT=80
BACKEND_PORT=8000

# Optional: If you want to expose PostgreSQL externally
# POSTGRES_PORT=5432
