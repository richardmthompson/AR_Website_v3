# =============================================================================
# AR Automation - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values below
# Never commit your .env file to git (it contains secrets!)
#
# For Development: Use local Docker database (default setup below)
# For Production:  Use Neon cloud database and update accordingly
# =============================================================================

# Database Configuration
# -----------------------------------------------------------------------------
# DEVELOPMENT (default): Use local PostgreSQL in Docker
DATABASE_URL=postgresql://postgres:postgres@db:5432/ar_automation

# PRODUCTION: Uncomment and configure Neon database instead
# DATABASE_URL=postgresql://username:password@host.neon.tech:5432/dbname?sslmode=require

# Local PostgreSQL Settings (used by Docker)
POSTGRES_DB=ar_automation
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# API Keys
# -----------------------------------------------------------------------------
# REQUIRED: Get your OpenAI API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# REQUIRED: Get your Resend API key from https://resend.com/api-keys
RESEND_API_KEY=re_your-resend-api-key-here

# REQUIRED: Email address for internal lead notifications
INTERNAL_NOTIFICATION_EMAIL=team@arautomation.com

# OPTIONAL: LangChain tracing (for debugging AI agent)
LANGCHAIN_API_KEY=your-langchain-api-key-here
LANGCHAIN_TRACING_V2=false
LANGCHAIN_PROJECT=ar-automation

# Application Environment
# -----------------------------------------------------------------------------
# Use 'development' for local work, 'production' for deployment
ENVIRONMENT=development
NODE_ENV=development

# Port Configuration
# -----------------------------------------------------------------------------
# Frontend: 3000 for dev (80 requires admin on Mac/Linux)
# Backend:  8000 (default)
FRONTEND_PORT=3000
BACKEND_PORT=8000

# =============================================================================
# Quick Start Checklist:
# 1. Copy this file: cp .env.example .env
# 2. Add your OPENAI_API_KEY above
# 3. Run: docker-compose up --build
# 4. Visit: http://localhost:3000
# =============================================================================
